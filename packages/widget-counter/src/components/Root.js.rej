diff a/packages/widget-counter/src/components/Root.js b/packages/widget-counter/src/components/Root.js	(rejected hunks)
@@ -6,12 +6,12 @@ const Root = () => {
-  const { counter } = useSelector(s => s.counter)
-  console.log(counter)
-  const d = useDispatch()
-  const inc = () => {
-    d(actions.counter.increment())
-  }
-  const dec = () => {
-    d(actions.counter.decrement())
-  }
-  return (
-    <div style={{ border: '1px solid grey', margin: 20, padding: 20 }}>
-      <div>{counter}</div>
+	const { counter } = useSelector(s => s.counter);
+	console.log(counter);
+	const d = useDispatch();
+	const inc = () => {
+		d(actions.counter.increment());
+	};
+	const dec = () => {
+		d(actions.counter.decrement());
+	};
+	return (
+		<div style={{ border: '1px solid grey', margin: 20, padding: 20 }}>
+			<div>{counter}</div>
@@ -19,5 +19,5 @@ const Root = () => {
-      <button onClick={inc}>+</button>
-      <button onClick={dec}>-</button>
-    </div>
-  )
-}
+			<button onClick={inc}>+</button>
+			<button onClick={dec}>-</button>
+		</div>
+	);
+};
